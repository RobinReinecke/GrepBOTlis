<Controls:MetroWindow x:Class="GrepBOTlis.Ui.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        BorderBrush="{DynamicResource AccentColorBrush}"
        BorderThickness="1"
        Title="{Binding WindowTitle, Mode=OneWay}" Height="350" Width="525"
        DataContext="{Binding Main, Source={StaticResource Locator}}">
<Controls:MetroWindow.RightWindowCommands>
    <Controls:WindowCommands>
        <Button Content="Settings"></Button>
    </Controls:WindowCommands>
</Controls:MetroWindow.RightWindowCommands>
    <Grid x:Name="LayoutRoot">
        <DockPanel >
            
            <TabControl>
                <TabItem Header="{Binding BrowserTabTitle}">
                    <ContentControl Content="{Binding Browser, Source={StaticResource Locator}}" />
                </TabItem>
                <TabItem Header="{Binding LogTabTitle}"></TabItem>
            </TabControl>
            
        </DockPanel>
    </Grid>
</Controls:MetroWindow>



